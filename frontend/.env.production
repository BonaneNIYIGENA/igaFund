# Replace <your-service> with your actual Render service name after first deployment.
# Must end in /api — the backend mounts every blueprint under that prefix.
VITE_API_URL=https://<your-service>.onrender.com/api
